
<div class="carousel-band">
  <div class="carousel">
    <div class="carousel-container">
      {{#each projects}}
          <div class="slide">
              <div class="slide-text">
                  <h2 class="slide-title">
                      <a href="/project/{{this.slug}}">
                          {{this.name}}
                      </a>
                  </h2>
                  <p class="slide-description">{{this.descriptions.[0].content}}</p>
              </div>
              {{#if this.descriptions.[0].images.[0]}}
                  <div class="slide-image-wrapper">
                      <img 
                          src="{{this.descriptions.[0].images.[0].url}}" 
                          alt="{{this.descriptions.[0].images.[0].alt}}" 
                          class="project-image"
                      >
                  </div>
              {{/if}}
          </div>
      {{/each}}
    </div>
  </div>

  <div class="carousel-controls">
    <button class="control prev" onclick="prevSlide()">⬅</button>
    <div class="carousel-indicators">
      {{#each projects}}
        <div class="indicator" onclick="goToSlide({{@index}})"></div>
      {{/each}}
    </div>
    <button class="control next" onclick="nextSlide()">➡</button>
  </div>
</div>

<style>
.carousel {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-top: 40px;
}

.carousel-container {
  width: 1000px;
  overflow: hidden;
  position: relative;
}

.slide {
  display: none;
  /* no global text-align here, children will control alignment */
  /* establish flex container when visible */
}

.slide.active {
  display: flex;
  align-items: stretch;
}

.slide-text {
  flex: 1;
  display: flex;
  flex-direction: column;
  /* keep title and description closer together */
  justify-content: flex-start;
  gap: 30px; /* space between title and description */
  padding: 20px;
  text-align: left; /* ensure text is left-aligned */
}

.slide-title {
  margin: 0;
}

.slide-description {
  /* pull the description up a bit if needed */
  position: relative;
  top: -10px;
  margin: 0;
  text-indent: 1.5em; /* add paragraph indent */
  /* alternatively you could use padding-left */
}

.slide-image-wrapper {
  flex: 1;
  overflow: hidden;
  /* add space inside so the image doesn't touch the slide border */
  padding: 15px;
  box-sizing: border-box; /* ensure padding is included in flex sizing */
}

.project-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 10px; /* round all four corners */
  /* if padding is added to wrapper we may need to remove the rounding on the inner image
     or keep same to avoid clipping. adjust as necessary. */
}

.carousel-indicators {
  display: flex;
  justify-content: center;
  margin-top: 10px;
  gap: 5px;
}

.carousel-indicators .indicator {
  width: 16px;
  height: 16px;
  background-color: #ccc;
  cursor: pointer;
}

.carousel-indicators .indicator.active {
  background-color: #333;
}

.carousel-controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  margin-top: 10px;
}

.carousel-controls .control {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
}

.carousel-controls .control.prev {
  margin-right: 10px;
}

.carousel-controls .control.next {
  margin-left: 10px;
}

/* new band around carousel + controls */
.carousel-band {
  background: #f0f0f0;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 8px;
  max-width: 600px;
  margin: 0 auto;
}

/* slide content block */
.slide {
  display: none;
  text-align: center;
  /* establish flex container when visible */
  border: 1px solid #bbb;
  border-radius: 8px;
  background: #fff;
  overflow: hidden;
}

</style>

<script>
let currentIndex = 0
const slides = document.querySelectorAll('.slide')
const indicators = document.querySelectorAll('.indicator')

function updateIndicators(index) {
  indicators.forEach(ind => ind.classList.remove('active'))
  if (indicators[index]) {
    indicators[index].classList.add('active')
  }
}

function showSlide(index) {
  slides.forEach(slide => slide.classList.remove('active'))
  slides[index].classList.add('active')
  updateIndicators(index)
}

function nextSlide() {
  currentIndex = (currentIndex + 1) % slides.length
  showSlide(currentIndex)
}

function prevSlide() {
  currentIndex = (currentIndex - 1 + slides.length) % slides.length
  showSlide(currentIndex)
}

function goToSlide(index) {
  if (index >= 0 && index < slides.length) {
    currentIndex = index
    showSlide(currentIndex)
  }
}

if (slides.length > 0) {
  showSlide(currentIndex)
}
</script>
